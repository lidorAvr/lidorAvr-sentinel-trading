# Phase ALGO-2A — SCOPE / איפיון (ALGO live-vs-backtest divergence: read-only edge-shape statistic + dual-surface labelled display, ZERO alerts)

**Status:** SCOPE — founder-approved via the cross-team CEO report (3 decisions taken: ① symbol→strategy mapping **founder-confirmed "identical per symbol"** ⇒ the symbol join is permitted, but it is still surfaced as *"אותה-אסטרטגיה לפי אישור המנהל — לא מאומת אוטומטית במערכת"* (founder-asserted, NOT system-verified) — never as proven truth; ② Phase-2a = read-only edge-shape divergence statistic + honest labelled display, **ZERO alerts/ZERO push**, all 6 honesty disclaimers, hard min-sample gate, symbol-keyed; **all observational alerting deferred to a separate founder-gated Phase-2b**; ③ surface = **BOTH** the Telegram ALGO panel and the dashboard ALGO-backtest panel, rendered from **ONE pure formatter** (single source of truth) so the two surfaces are byte-identical and cannot drift). Governed, Mark-gated, parent-verified, exact-CI post-commit on the CLEAN tree. Live baseline HEAD `ef0a2d1` (post-JOURNAL-1), suite **2242/0 cov 73.04%**. No live financial values / no proprietary strategy data in any committed file. Author the SCOPE → founder approval → BUILD (tree left dirty) → independent parent verification → exact CI post-commit clean tree → push → IMPL.

## 0. Founder binding guidance (encoded literally, non-negotiable)
"Backtest = machine-executed HISTORY, NOT real trades; a directional general baseline only; never rely on it blindly without added reasoning." Every committed figure and every surfaced line MUST be edge-shape, explicitly labelled non-truth, require human judgement, carry zero actuation. The founder's "identical per symbol" confirmation removes only the *unrelated-edges* worst case; it does NOT upgrade the backtest to a forward promise.

## 1. The divergence — what is honestly comparable (cross-team A)
Live ALGO is a clean provable split: `engine_core.is_algo_position` / `setup_type=="ALGO"` → `STAT_BUCKET_ALGO`; `algo_metrics.build_algo_cohort` already isolates it. Backtest stats already exist: `algo_backtest_store.compute_algo_backtest_stats` (per strategy: `n`, `win_rate_pct`, `avg/median/sum_return_pct`, `profit_factor`, `expectancy_pct`, `max_trade_drawdown_pct`, exit-mix, streaks, date_span). **Join = by SYMBOL only** (HOOD/JPM/PLTR/QQQ/TSLA — exactly one backtest strategy each; live trades carry no strategy/codename field — confirmed: no other key exists). Honestly comparable (edge-shape, per symbol): win-rate, median/avg Return %, profit-factor, loss-streak, exit-reason mix — **shape only**. NOT comparable and NEVER claimed: account P&L, sizing, slippage/fees (`Volume=1`,`cost=0%`), long-only-vs-live, differing windows/regime, sample asymmetry (backtest 18–98 vs live capped ~30).

## 2. W-2A1 — pure divergence helper (NEW module, read-only)
New root module (e.g. `algo_divergence.py`), **not byte-locked**, **zero import of** `engine_core`/`analytics_engine`/`period_data_probe`/Supabase/network. Pure deterministic function: input = the existing live ALGO cohort aggregates (read via existing `algo_metrics` helpers — WR, median/avg Return %, PF, loss-streak) + `compute_algo_backtest_stats` output; output = a per-symbol structured delta + **ONE pure formatter** that emits the exact display text. Idempotent, no state, no write, never raises (boundary-input discipline; missing/empty side ⇒ honest empty marker, never zero-as-truth).

## 3. Honesty gates (cross-team B) — each gates the claim AND forces a Hebrew disclaimer
1. **Hard min-sample gate** (mirror `algo_metrics.ALGO_COHORT_WINDOW=30` discipline): below the live-N floor ⇒ emit *"אין מספיק מדגם חי — לא מוצג הפרש (לא חוסר, לא איתות)"*, **never silent-zero, never a delta**. 2. Date-window/regime mismatch ⇒ *"חלון בקטסט שונה ממסחר חי — אינדיקטיבי בלבד"*. 3. Survivorship/look-ahead ⇒ *"בקטסט עלול לכלול הטיית-היסטוריה"*. 4. `Volume=1`/`cost=0`/no-slippage ⇒ reuse existing `BACKTEST_LABEL` *"בקטסט — לא חי, לא הבטחה קדימה; ללא עמלות/החלקה"*. 5. long-only-vs-live ⇒ *"אסטרטגיות לונג-בלבד — לא חופף למסחר החי"*. 6. multiple-comparisons (5 strategies) ⇒ *"5 השוואות — חריגה בודדת אינה הוכחה"*. **Plus** the mandatory join banner: *"הצמדה לפי-סימול · אותה-אסטרטגיה לפי אישור המנהל · לא מאומת אוטומטית"* and the existing `OBSERVE_ONLY_LABEL`.

## 4. observe-only — INVIOLABLE (cross-team B; AGENTS.md #8 / DEC-20260511-001)
ZERO directive, ZERO push, ZERO Supabase write, ZERO state mutation. Max actionability = display/disclose only — **never** a recommendation; max severity wording "דרוש עיון", **never "דרושה פעולה"**. Divergence NEVER enters WR/Expectancy/PF/Net-R/edge/headline. Neutral `🔭` only — **no 🔴/🟢 verdict colour**, no imperative verb (do NOT model on the borderline `telegram_tasks.py` time-exit line). No new message TYPE.

## 5. W-2A2 — dual-surface integration (founder chose BOTH; single source of truth)
**Anti-drift requirement (the core "both" risk):** both surfaces call the **same pure formatter** from W-2A1 — neither surface formats independently. (a) **Telegram** `telegram_tasks.py handle_algo_panel` — ONE extra descriptive `🔭` line per symbol, AFTER the existing per-symbol state line, BEFORE the existing mandatory backtest caveat; rendered ONLY when the user opens the panel (zero unsolicited push). (b) **Dashboard** ALGO-backtest panel in `dashboard.py` — ONE additive marker-delimited section. A dedicated test asserts the two surfaces emit **byte-identical** divergence text for the same input.

## 6. Strategy-IP (W2 / ALGO-BT-1 precedent)
Real TrendSpider CSVs stay git-ignored (`data/algo_backtests/`). Committed = helper + dual-surface wiring + tests on **synthetic fixtures only**. No real Return%/symbol rows, no derived figure leaking the proprietary edge. `git check-ignore data/algo_backtests/<sample>` verified post-commit.

## 7. Hard constraints — git-diff EMPTY (auto-FAIL)
`engine_core.py`, `analytics_engine.py`, `period_data_probe.py`, LOCKED `tests/test_real_data_april_regression.py`, every `tests/_byte_lock_baselines/*`, `docker-compose.yml`, `telegram_bot_secure_runner.py`, migrations, `templates/*`, `risk_monitor.py`, `telegram_bot.py`, `supabase_repository.py` → git-diff EMPTY. **Permitted diff ONLY:** new `algo_divergence.py` (or equivalently-named helper), additive section in `dashboard.py`, additive `🔭` line(s) in `telegram_tasks.py handle_algo_panel`, new `tests/test_phase_algo2a.py`. No schema/migration, no Supabase mutation, no new message TYPE, ALGO observe-only intact.

## 8. Proof obligations (named — verified independently by parent)
(a) observe-only intact — helper imports no engine/analytics/Supabase/network; zero push/directive/Supabase. (b) backtest NEVER presented as truth — every figure carries `BACKTEST_LABEL`+`OBSERVE_ONLY_LABEL`+the 6 disclaimers+join banner. (c) symbol join surfaced as founder-asserted, NOT system-verified. (d) hard min-sample gate proven — below floor ⇒ honest "אין מספיק מדגם", never zero/delta. (e) **cross-surface byte-identity** — Telegram line text == dashboard section text from the single formatter. (f) no strategy CSV committed (`git check-ignore` post-commit). (g) existing report KPIs + **LOCKED April byte-identical** (8 / +$180.49 / WR .375 / PF 2.6262 / excl 2); existing dashboard + existing `handle_algo_panel` numbers/strings byte-identical (only an additive line/section). (h) full suite `python -m pytest -q` ≥ **2242, 0 failed** (new tests ADD-only; no existing test deleted/weakened — Mark 6.1; existing dashboard + telegram_tasks tests green unchanged). (i) **exact CI post-commit on the CLEAN tree:** `pytest --tb=short -q --cov=engine_core --cov=adaptive_risk_engine --cov=analytics_engine --cov=addon_risk_engine --cov-report=term --cov-fail-under=67` (CI env) → 0 failed, cov ≥ 67 (currently 73.04%).

## 9. Separate acceptance tests (`tests/test_phase_algo2a.py`, synthetic fixtures only)
Per-symbol delta correctness on a synthetic fixture; below-sample-floor ⇒ "אין מספיק מדגם" (NOT zero/delta); each of the 6 disclaimers present when its condition triggers; join banner ("לפי אישור המנהל · לא מאומת אוטומטית") always present; **cross-surface text byte-identity** (Telegram == dashboard from the one formatter); output contains no imperative/directive token and no 🔴/🟢; ALGO divergence never feeds WR/Expectancy/PF; helper has no Supabase/engine/analytics/network import (import-introspection test); existing `handle_algo_panel` + dashboard ALGO-panel tests pass unchanged.

## 10. Deferral list
**Phase-2a does NOT:** send any Telegram/digest/push alert; add a new message TYPE; touch `risk_monitor.py`/anti-spam state; mutate Supabase; add a directive/auto-management/threshold-action; change R/NAV/exposure/campaign math. **Deferred → Phase-2b** (separate governed, founder-gated): observational divergence *alert* on an existing surface, observe-only, no new TYPE — only after 2a's honesty gates are proven live. **Standing deferred reminders:** 🔴 SECURITY L-1 + Telegram token rotation; OPS-1; OPS-2; open Phase-2b.

## 11. Done = deploy-ready
Phase-2a landed, parent-verified, full CI-equivalent post-commit on the CLEAN tree 0-failed, cross-surface byte-identity + observe-only + backtest-not-truth + LOCKED April + report KPIs proven, no byte-locked/`supabase_repository.py`/`telegram_bot.py`/`risk_monitor.py` touched, no strategy CSV committed, `docs/teams/PHASE_ALGO2A_IMPL.md` written. Then return to the founder with ONE deploy command (rebuild **both** `dashboard` + `telegram-bot` services) + the standing deferred reminders + the open Phase-2b note.
