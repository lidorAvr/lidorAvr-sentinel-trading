# Phase ALGO-BT-1 — SCOPE / איפיון (ALGO backtest baseline: ingest + statistics + read-only surface)

**Status:** SCOPE — founder-approved staged Phase-1 (4 scoping decisions taken: ① staged Phase-1 = ingest + per-strategy statistics + read-only surface; ② in-repo + computed-on-load, NO DB/migration; ③ alerts observe-only & reuse existing surfaces — **deferred to Phase-2**, none in Phase-1; ④ idempotent recurring re-ingest). Governed, Mark-gated, parent-verified, exact-CI post-commit on the clean tree. Live baseline HEAD `0672b0c` (post-REPORT-1), suite **2205/0 cov 73.04%**. No live financial values / no proprietary strategy data in any committed file.

## Source data (reviewed, read-only)
Founder TrendSpider **Strategy Tester backtest** export (12 May 2026): 5 long strategies, one per symbol — HOOD 21×9 EMA 2H (34), JPM 7×34 SMA 2H (33), PLTR NEW 30MIN (18), QQQ 6×30 SMA 2H (98), TSLA 6×34 SMA 2H (49). 22-col schema (`Symbol,Direction,Volume,Entry/Exit Candle Open Time(+unix),Entry/Exit Price,Trade cost,Closed?,Entry Reason,Exit Reason,Length (candles),Return %,Max Gain vs Entry %,Max Drawdown %,Max Gain/DD vs Entry %,…After Candles`). `Volume=1`, `Trade cost=0%` ⇒ these are **edge-shape** metrics (Return %/trade), NOT account P&L and NOT a forward promise. Signal codenames: entry Romeo/Foxtrot/Juliett/India; exit x_candles_passed/Zulu/take_profit/Kilo/stop_loss/Juliett. PNGs = human oracle for stat validation.

## Binding governance
- **ALGO observe-only is INVIOLABLE** (DEC-20260511-001 #8 / AGENTS.md #8). Phase-1 has **zero** alerts and **zero** directives — it is pure statistics + a read-only honest display *about an externally-managed bot's backtest*. Live-vs-backtest divergence + observational alerts are **Phase-2** (separate governed Phase).
- **Strategy-IP protection (W2 doctrine).** The repo is pushed to GitHub; the founder's real backtest CSVs are proprietary. Per the established `sentinel_config.json` precedent (in-repo path, **git-ignored**, only an `.example` tracked), the real data dir is git-ignored and NEVER committed. Committed = loader + stats + read-only panel + tests on a **synthetic** fixture only. This fully satisfies "in-repo + computed-on-load, no DB/migration".
- NO byte-locked file modified: `analytics_engine.py`, `engine_core.py`, `period_data_probe.py`, LOCKED `tests/test_real_data_april_regression.py`, all `tests/_byte_lock_baselines/*`, `docker-compose.yml`, `telegram_bot_secure_runner.py`, migrations → git-diff EMPTY. **No migration, no Supabase mutation, no new message TYPE.** Sprint-22..30 + C1/C2/B3/Arch-F1/NAV-Unify/W1/W3/ALGO-1/2/3/REPORT-1 invariants intact; every existing report KPI + LOCKED April byte-identical (Phase-1 is purely additive — a new module + an additive dashboard panel).

## W-BT1 — data location & idempotent re-ingest (in-repo, git-ignored)
Canonical in-repo dir `data/algo_backtests/<SYMBOL>/<strategy>.csv` (the founder drops/replaces TrendSpider exports here). `.gitignore` adds `data/algo_backtests/` (mirroring `sentinel_config.json`). Tracked: `data/algo_backtests/README.md` (expected layout + the 22-col schema contract) + `.gitkeep`. **Idempotent re-ingest:** stats are a pure deterministic function of the files PRESENT at load — re-running on identical files yields identical output; replacing/adding a file replaces/adds exactly that strategy's stats; removing a file removes it; **no accumulation, no stateful store, no DB**. Missing/empty dir ⇒ empty result, honest "אין נתוני בקטסט ALGO טעונים", never raises.

## W-BT2 — loader/parser (new `algo_backtest_store.py`, not byte-locked)
Read-only deterministic parser for the 22-col TrendSpider schema: robust UTF-8 path handling (the encoded Hebrew folder), IST/IDT timestamps, numeric coercion (`Return %`, `Max Drawdown vs Entry %`, `Length (candles)`), `Closed?` filter, entry/exit-reason normalization, strategy id derived from filename + `Symbol`. Malformed/extra columns ⇒ skipped-with-honest-note, never a hard crash (boundary-input discipline). Pure read; no network, no Supabase, no write.

## W-BT3 — per-strategy statistics (BACKTEST-labeled, observe-only)
Computed-on-load per strategy: N, Win-Rate %, avg & median Return %, sum Return %, Profit-Factor (Σ win% / |Σ loss%|), expectancy proxy (mean Return %), max single-trade drawdown (`Max Drawdown vs Entry %`), avg/max holding length (candles), exit-reason mix (take_profit / stop_loss / time-stop / signal), date span, longest win/loss streak. Every figure explicitly tagged **בקטסט — לא חי, לא הבטחה קדימה** and **ALGO · מנוהל חיצונית · פיקוח בלבד** (the honesty + observe-only doctrine). No R/NAV/account math, no coupling to live ALGO/Supabase state.

## W-BT4 — read-only surface (additive, no new message type)
An additive **dashboard** read-only panel "ALGO — בסיס בקטסט (פיקוח בלבד)" rendering the W-BT3 per-strategy table with the BACKTEST/observe-only labels and an honest empty-state. Additive only (Sprint-27 W1 made the dashboard canonical/read-only); zero change to existing dashboard numbers or any Telegram/report surface; no new message TYPE. A pure text formatter (`format_algo_backtest_summary`) is also provided so **Phase-2** can reuse it on an existing Telegram/digest surface without rework.

## Separate acceptance tests (`tests/test_phase_algobt1.py` + synthetic fixture)
Synthetic fixture (small hand-crafted CSVs in the 22-col schema under `tests/_fixtures/algo_backtests/` — NO real data): parser correctness; stats correctness vs hand-computed WR/PF/avg/maxDD/streak; **idempotency** (load×2 byte-identical; replace a file ⇒ only-current reflected, no accumulation; remove ⇒ gone); graceful empty/missing dir (no raise, honest empty); UTF-8/encoded-folder + IST/IDT parsing; malformed-row skipped-not-crash; BACKTEST + observe-only labels present; dashboard panel renders read-only and never touches live ALGO/Supabase; LOCKED April + all `_byte_lock_*` unaffected. No existing test deleted/weakened (Mark 6.1).

## Hard constraints (auto-FAIL)
Byte-locked set + baselines + migrations + compose + secure_runner git-diff EMPTY. No migration, no Supabase mutation, no new message TYPE, no ALGO directive/alert in Phase-1 (observe-only). Real backtest data NEVER committed (git-ignored). Every existing report KPI + LOCKED April + Sprint-22..30/ALGO-1..3/REPORT-1 byte-identical. Full suite `python -m pytest -q` ≥ **2205**, 0 failed (new tests only ADD). Exact CI command (CI env) post-commit on the clean tree → 0 failed, cov ≥67.

## Done = deploy-ready
W-BT1..BT4 landed, parent-verified, full CI-equivalent post-commit clean-tree 0-failed, byte-locked + LOCKED April + report KPIs byte-identical, real data git-ignored, `docs/teams/PHASE_ALGOBT1_IMPL.md` written. Then return to the founder with: ONE deploy command, the host one-time step to place the real CSVs into the git-ignored `data/algo_backtests/`, the explicit Phase-2 hand-off (live-vs-backtest divergence + observe-only alerts), and the standing deferred reminder (L-1/token rotation, OPS-1/2).
