# Sprint 24 — Architecture Structure Audit (Wave-1, DOC-ONLY)

**Date:** 2026-05-16 · **Branch:** `claude/review-system-audit-FBZ2h` · **Team:** 🏗️ Architecture
**Scope:** behavior-preserving structure findings only. NO new features, NO behavior/wording/output-byte change, NO math change. DOC-ONLY — no code, no commit, no compose/migration. Baseline full suite **1879**.

## ⟨MARK⟩ — UNRESOLVED POLICY (binding, not yet authored)
`docs/teams/MARK_SPRINT24_RULINGS.md` is **absent** at audit time. Every scope/risk/byte-identical policy below is **provisional** and DEFERS to verbatim `⟨MARK:…⟩` once Mark issues Sprint-24 rulings. No item here is an execution authorization — Wave-2 is Mark-gated + founder-tier-gated. Fragile-area items (engine_core / telegram_bot / NAV config / docker-compose) are **HIGH** and require explicit founder go-ahead per DEC-20260516-021 / SPRINT24_PLAN §14.

## Method
Read CLAUDE.md, AGENTS.md, SPRINT24_PLAN, DEC-019/-020/-021; surveyed senders/splitters, coerce/tz loops, period helpers, the named extraction-candidate modules. Conservative bias: a few high-confidence safe wins over a long risky list. All evidence is real `file:line` (verified by reading).

## Prioritized findings

| # | Area | file:line evidence | Problem | Proposed behavior-preserving change | Risk | Value | Byte-identical proof strategy | Tests needed |
|---|------|--------------------|---------|--------------------------------------|------|-------|-------------------------------|--------------|
| 1 | Dead/named-stub clarity | `telegram_formatters.py:835 algo_dead_money_rule`; only caller `tests/test_sprint15_r_integrity.py:296` | Public-looking fn, no production caller; pure `*args/**kwargs` sentinel stub. Not dead (test-pinned, Mark §5 #5 named) but its non-production status is undocumented at the call boundary. | DOC-ONLY: add a one-line "no production caller; test-pinned stub" note. No signature/return/byte change. (Deletion is OUT-OF-SCOPE: WS-C/ALGO-gate deferred, would break the pinned test.) | low | low | Body/return unchanged → `test_sprint15_r_integrity.py::test_algo_dead_money_rule_is_pending_stub` still asserts `"pending founder rule"`; full suite 1879. | Existing test is the proof; add 0. |
| 2 | Duplication — config reader | `bot_helpers.py:80-85 get_account_settings` vs `risk_monitor.py:150-153 get_account_settings` (byte-equivalent: same `open("sentinel_config.json")`, same fallback `{"total_deposited":7500.0,"risk_pct_input":0.5}`) | Two independent definitions of the NAV/risk config reader → drift risk (a future fallback edit in one not the other distorts risk/exposure — a "most fragile area" per CLAUDE.md). | SMALL gradual extraction (CLAUDE.md "risk/NAV config helper"): `risk_monitor` imports `bot_helpers.get_account_settings` instead of its local copy; delete the duplicate. Single source of truth, identical observable behavior. | **HIGH** (NAV/risk config = CLAUDE.md fragile area; founder-gated) | high | Diff both bodies are token-identical incl. fallback dict + exception breadth; new test asserts both call sites return identical dict for present/missing/corrupt config; `risk_monitor` import-graph unchanged otherwise; full suite 1879. | New `test_account_settings_single_source.py` (present/missing/corrupt-JSON parity) before the edit. |
| 3 | Duplication — numeric coerce loop | `analytics_engine.py:30-33` vs `period_data_probe.py:164-168` (probe `:161-162` comment already says "the SAME numeric coerce as analytics_engine.py:30-33") | Same `pd.to_datetime(...,errors="coerce")` + per-col `pd.to_numeric(...).fillna(0)` written twice (probe adds `initial_risk_price`). Self-documented duplicate; divergence in the col list is a real maintenance hazard on a math-bearing path. | Candidate ONE shared **pure** helper reused by both (+ the Sprint-22 `_to_naive` already shared). **Engine team OWNS the math-bearing byte-identical proof** (locked April regression + tz-aware==naive). Architecture flags the structure only; coordinate, do NOT duplicate Engine's analysis. Note col-list difference must be preserved exactly (param, not unified). | **HIGH** (engine_core/analytics_engine math-fragile; founder-gated; cross-team) | high | DEFER to Engine's `SPRINT24_ENGINE_AUDIT.md` proof: LOCKED `test_real_data_april_regression.py` + tz-aware==tz-naive + full suite 1879, byte-identical metrics dict AND byte-identical probe string. | Engine-owned; arch adds none. |
| 4 | Duplication — Telegram senders/splitters | `main.py:55 send_telegram` (requests, no parse_mode, no split, no retry); `risk_monitor.py:540 send_telegram`/`:545 send_telegram_with_keyboard` (telebot, Markdown, no split); `telegram_portfolio.py:21 _send_long_message` (telebot, Markdown, 3900-split at 〰️/\n, markup last); `telegram_bot.py:61 _send_probe_chunks` (telebot, **NO** parse_mode, 3900-split at glue/\n, RTL re-prefix, markup last); `report_delivery.py:14 send_summary` (requests, Markdown, retry/backoff) | 5 senders, **deliberately divergent** on transport (requests vs telebot), parse_mode (none/Markdown/none), splitting (none/〰️/glue), retry (none/none/3×backoff). Real overlap is the *split-at-boundary* shape (`_send_long_message` vs `_send_probe_chunks`). | NO unification of transport/parse_mode/retry (those differences are load-bearing & production-validated — unifying = behavior change, OUT-OF-SCOPE). Narrow safe win: extract ONE pure `split_for_telegram(text, limit, sep)→list[str]` string-only helper; `_send_long_message` & `_send_probe_chunks` call it with their own params; the SEND loops (parse_mode, RTL re-prefix, markup-last) stay in each caller untouched. | **HIGH** (`telegram_bot.py` fragile + Sprint-23 production-validated loss-free probe; founder-gated) | med | New helper returns the SAME `list[str]` each caller currently builds → identical `bot.send_message` arg sequence per call site. Prove: `test_sprint23_probe_split.py` byte-identical part boundaries + a parametrized table reproducing today's exact parts for both helpers (short→1 part; glue; oversized-single-line whole); full suite 1879. **Probe path stays Sprint-23-locked byte-identical.** | New `test_telegram_split_helper.py` (parity vs current `_send_long_message`/`_send_probe_chunks` outputs) before any extraction. |
| 5 | Doc/clarity defect (no behavior) | `report_scheduler.py:115-116` docstring "4-week lookback" vs `:132 lookback = period_start - timedelta(weeks=8)` | Docstring contradicts code (8-week actual). Pure stale-doc; misleads maintainers about period/lookback contract. | DOC-ONLY: correct docstring to "8-week lookback". ZERO code/behavior change. (Hyperscaler OPS audit also flags this — single owner, no duplicate edit.) | low | med | Only a comment line changes; AST of `_fetch_trades_df` unchanged; `test_report_scheduler.py` + full suite 1879 unchanged. | Existing scheduler tests suffice; add 0. |
| 6 | Module-boundary smell — repo-layer leak | `telegram_bot.py:769 res = supabase.table("trades").select("*").execute()` (only direct `.table(` in `telegram_bot.py`; `supabase_repository.py` already exists w/ `get_all_trades(sb):22`) | One residual raw Supabase read bypasses the extracted repository layer (CLAUDE.md "extract Supabase repository layer"). Inconsistent with every other read already routed through `supabase_repository`. | SMALL gradual extraction: replace the inline `.select("*")` with `repo.get_all_trades(supabase)` (read-only, same query semantics: `select("*")`). One-line, additive-style, mirrors the proven prior repo extraction. | **HIGH** (`telegram_bot.py` fragile area; founder-gated) | med | `supabase_repository.get_all_trades` already issues identical `select("*")` (verify no `.order`/filter difference); mock-Supabase test asserts identical query call + identical returned `DataFrame`; `test_supabase_repository.py` + addon flow tests + full suite 1879. Read-only flow → no Supabase mutation (AGENTS.md #4 preserved). | Extend `test_supabase_repository.py` / add addon-flow parity test before the edit. |
| 7 | Inconsistent error-handling pattern (doc-only observation) | `risk_monitor.py:541-548` `except Exception as e: print(...)`; `report_delivery.py:_log`; `main.py:66 log(...)`; `bot_helpers.py:38 except: pass` | Send failures handled 3 different ways (`print` / `_log` file / `log` file / silent). Not a bug; a maintainability/observability smell. | OUT-OF-SCOPE to change this sprint (any logging-channel change risks altering operator-visible behavior / log-noise contract — defer to Hyperscaler OPS audit's log-level review; behavior-preserving only). Logged here as a known boundary smell, **no edit proposed**. | n/a | n/a | n/a (no change) | n/a |

## Explicitly OUT-OF-SCOPE (tempting but a behavior change — NOT cleanup)
- Unifying sender **transport/parse_mode/retry** (#4) — divergence is production-validated & load-bearing; unifying changes output bytes / delivery semantics.
- Deleting `algo_dead_money_rule` (#1) — WS-C/ALGO-gate DEFERRED (DEC-020); test-pinned.
- Any probe (`period_data_probe.py`) edit — Sprint-23-locked byte-identical (DEC-020); chunk-never-truncate (#1) invariant.
- Logging-channel normalization (#7); the misleading ALGO "תקן entry/stop" wording (DEC-020 — pre-existing, a behavior change to alter).
- Any R/NAV/exposure/campaign/Expectancy math; admin/dev-PIN gate; secure_runner; `telegram_bot.py` wholesale rewrite; compose/migration/schema.

## Recommendation
Tier-A safe-now (DOC-ONLY, byte-trivial): **#1, #5**. Tier-B (test-backed small extraction, founder-gated): **#2, #6, #4-split-helper**. Tier-C (HIGH, cross-team, Engine-owned proof): **#3**. Prefer founder default = Tier-A only, then reassess — pending ⟨MARK⟩ Sprint-24 rulings.
