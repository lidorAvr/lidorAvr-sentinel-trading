# Sprint 23 — Plan: probe "message too long" (Telegram 400) — formatting/delivery-only fix

**Date:** 2026-05-16 · **Branch:** `claude/review-system-audit-FBZ2h`
**Trigger:** DEC-20260516-020. Founder: "תקן Probe (Sprint-23)".
**Structure:** Wave 1 (parallel, doc-only) → checkpoint → Wave 2 (build) → consolidation. Mark-gated-light (formatting/delivery boundary; not engine/analytics math — but a BINDING AST READ-ONLY/no-secrets contract + admin-gated handler are in scope).

## Proven root cause (do not relitigate)
`telegram_bot.py:318` sends `period_data_probe.build_probe_report()`'s FULL string in ONE `bot.send_message`. `build_probe_report(None)` = `_RTL + weekly + "\n\n" + _RTL + monthly`; each `_window_block` emits a per-campaign line for EVERY closed campaign + the WS-C candidate block ⇒ ~20 campaigns × 2 windows > Telegram 4096 → `Bad Request: message is too long`. Failed on LENGTH (not `Invalid comparison`) ⇒ the Sprint-22 tz-mirror plausibly held. Pure formatting/delivery defect.

## The fix (formatting/delivery boundary ONLY)
Split the probe output into ≤limit parts at safe boundaries, sent by the CALLER (the probe's binding contract: delivery is the caller's job; the probe NEVER sends/persists — AST-proven by `tests/test_sprint21_wave2.py`). Keep `period_data_probe.py` 100% untouched (Sprint-22 tz-mirror + honest empty/fail + §A1/§A3 AST proof byte-identical). An existing proven splitter — `telegram_portfolio.py:21 _send_long_message` (3900-char, separator/newline-aware, `reply_markup` last-part-only) — exists but forces `parse_mode="Markdown"` while the probe is PLAIN-TEXT (`campaign_id` `_` → Markdown 400). Mirror its proven shape WITHOUT changing the probe's plain-text send. **#1: chunk, never truncate** — hiding real rows is a #1 violation.

## Hard constraints (whole sprint)
No information loss (chunk ≠ truncate, #1). `period_data_probe.py` byte-identical. Plain-text send preserved (NO `parse_mode` change). Admin/dev-PIN gate (`telegram_bot.py:147-153`) untouched; no `telegram_bot.py` wholesale rewrite (one additive splitter in the existing dev-menu handler); no secure_runner bypass; no migration/compose/schema. WS-C DEFERRED; #8 untouched. Preserve 920be95/bcf32f5/Sprint-16..22 + WS-B `unlinked_*` + the Sprint-22 tz fix. Baseline full suite **1864**.

## Wave 1 — task distribution (parallel, doc-only)
- **🧠 Mark (lead — gates Wave 2):** `MARK_SPRINT23_RULINGS.md` — BINDING rulings: (1) **#1 chunk-not-truncate** — the probe must lose ZERO real rows; rule out any "show first N" trimming; the split must be loss-free and re-assemblable by a reader. (2) **Which side splits** — confirm the SEND/split stays in the CALLER (telegram_bot.py dev-handler), `period_data_probe.py` BYTE-IDENTICAL (so the §A1 READ-ONLY + §A3 no-secrets AST test in `tests/test_sprint21_wave2.py` + the Sprint-22 tz-mirror stay untouched); explicitly forbid the probe gaining any send/persist. (3) **Plain-text invariant** — the multi-send MUST keep the current `bot.send_message` plain-text behaviour (NO `parse_mode`); rule that reusing `_send_long_message` verbatim is FORBIDDEN (its `parse_mode="Markdown"` would break `_` in `campaign_id`). (4) **Split-boundary policy** — split at the probe's natural boundaries (between weekly/monthly `"\n\n"+_RTL`; within a window at line `\n`), max part ≤ a stated safe limit (recommend 3900, matching the existing helper); each part independently RTL-correct; `reply_markup` (dev menu) on the LAST part only. (5) **Admin gate** — the splitter lives inside the EXISTING dev-PIN handler region; no new auth path / no secure_runner bypass / no telegram_bot.py wholesale rewrite. (6) Confirm WS-C DEFERRED + #8 + 920be95/bcf32f5/Sprint-16..22 + Sprint-22 tz fix untouched. 10-item pass/fail Wave-2 gate checklist incl. probe byte-identical, AST READ-ONLY test still green, no parse_mode, no row loss, admin gate intact, full suite ≥1864 + new split tests.
- **🏗️ Architecture + Engine:** `SPRINT23_DESIGN.md` — exact patch design: a small loss-free splitter (mirroring `telegram_portfolio.py:21`'s proven 3900/separator/newline/last-markup shape, but `parse_mode`-free) invoked from the probe dev-handler at `telegram_bot.py:316-325` replacing the single `bot.send_message`; the exact boundary algorithm (prefer the `"\n\n"+_RTL` window split, then `\n`, never mid-line/mid-campaign so no row is split or lost); RTL-prefix correctness per part; `reply_markup=get_developer_menu()` last-part-only; the unchanged `except` path; confirm `period_data_probe.py` 0-diff. New tests design: a long synthetic probe string → asserts (a) every original non-empty line present across the concatenated parts (loss-free), (b) each part ≤ limit, (c) no `parse_mode`, (d) markup only on the last part, (e) the existing `tests/test_sprint21_wave2.py` AST READ-ONLY/no-secrets test still green, (f) probe file byte-identical. ⟨MARK⟩ slots verbatim for any threshold/policy. Baseline 1864; explicit "will NOT change" list (probe file, engine/analytics, parse_mode, admin gate, secure_runner, compose, migrations, Sprint-22 tz fix).
- **🚀 Hyperscaler:** `HYPERSCALER_SPRINT23_ADDENDUM.md` (≤90 words) — pure Telegram-formatting/delivery fix: no schema/migration (verify_migrations stays 005), no user_id, single-user byte-identical, host-agnostic, zero billing, no service-command change; chunking is per-message presentation only. 3-point consolidation checklist.

## Checkpoint
Parent independently verifies: `period_data_probe.py` git-diff EMPTY (byte-identical → Sprint-22 tz-mirror + §A1/§A3 AST proof intact); the splitter is loss-free (every probe line present across parts, no campaign row dropped/split); each part ≤ limit; NO `parse_mode` (campaign_id `_` safe); `reply_markup` last-part-only; admin/dev-PIN gate + handler structure intact (no telegram_bot.py wholesale rewrite); `tests/test_sprint21_wave2.py` READ-ONLY/no-secrets AST test green; WS-C DEFERRED + #8 + 920be95/bcf32f5/Sprint-16..22 + Sprint-22 tz fix untouched; full suite green (≥1864 + new split tests).

## Carried
🟢 Live smoke-test (Sprint 11–22) CLOSED (DEC-019 reconciliation). 🔴 NEW carried: founder runs the dev-PIN Probe post-deploy and sees the multi-part output deliver without the 400. WS-C reconsideration (ratified `initial_risk_price` contract). NULL-`campaign_id` founder repair runbook (Sprint-21). Per-user (Phase-B). ALGO Oversight Gate (DEC-20260515-014).
