# Sprint-24 Engine Audit — math-bearing code (DOC-ONLY, behavior-preserving)

**Date:** 2026-05-16 · **Branch:** `claude/review-system-audit-FBZ2h` · Wave-1, no code.
**Scope:** `analytics_engine.py`, `engine_core.py` (campaign-aggregation path), `period_data_probe.py` (read-only). DEC-20260516-021/-020/-019/-018, DEC-20260511-001(#8). CLAUDE.md most-fragile ⇒ **every item HIGH-risk by default**; conservative short list only.

⟨MARK⟩ `docs/teams/MARK_SPRINT24_RULINGS.md` is **absent** (parallel work). Scope/risk/value policy & the Wave-2 byte-identical gate **defer verbatim** to Mark's eventual `⟨MARK:…⟩`. Recommendations below are audit findings, NOT a go-ahead — fragile-area edits need explicit founder + Mark sign-off (DEC-021 process).

## Existing byte-identical pins (proof inventory)
- LOCKED `tests/test_real_data_april_regression.py` — April 8/+$180.49/WR.375/PF2.6262/excl2(1m,1a); weekly 0/3-ALGO/−$37.234.
- `tests/test_sprint22_tz_regression.py` — tz-aware==tz-naive **full-dict** equality (`test_april_aware_equals_naive_full_dict`, `test_weekly_aware_equals_naive_full_dict`, `test_scheduler_israel_tz_constant_equals_naive`); `_to_naive` identity-on-naive (`test_to_naive_identity_on_naive`); probe no-raise + `test_probe_to_naive_helper_is_shared_single_source`.
- `tests/test_sprint23_probe_split.py` — `period_data_probe.py` git-diff-empty (`test_period_data_probe_git_diff_empty`), loss-free.
- `tests/test_analytics_engine.py::TestAggregateGetCampaignRiskMetrics` + `test_stat_bucket.py` + `test_risk_basis_engine.py`.

## Prioritized findings

| # | file:line | problem | proposed behavior-preserving change | risk | value | EXACT byte-identical proof (named tests) | tests to add |
|---|-----------|---------|-------------------------------------|------|-------|------------------------------------------|--------------|
| 1 | `period_data_probe.py:163-168` vs `analytics_engine.py:30-33` (+ `engine_core.py:478`) | The numeric-coerce loop (`pd.to_datetime` + `pd.to_numeric(...).fillna(0)` over price/quantity/stop_loss/initial_stop/pnl_usd) is **duplicated verbatim** 3×; probe adds `initial_risk_price`. Maintenance hazard: a future column edit must touch 3 sites or silently diverge. | Extract ONE pure helper `_coerce_numeric(df, cols)` in `analytics_engine.py`; call from `analytics_engine` (cols incl. their set) and `period_data_probe` (cols incl. `initial_risk_price`). Identical pandas ops, identical column tuples per caller ⇒ provable identity. **Defer** `engine_core.py:478` (different col list `["quantity"…]`, no `trade_date`, different fillna context — DO NOT fold in). | **HIGH** (analytics is CLAUDE.md most-fragile; probe is Sprint-23 git-diff-locked → ANY probe byte change fails `test_period_data_probe_git_diff_empty`) | med (kills triple-duplication) | `test_real_data_april_regression.py` (both) + `test_sprint22_tz_regression.py` full-dict ×3 pin analytics side; probe side **blocked** unless Mark reclassifies the Sprint-23 byte-lock — flag as DO-NOT-TOUCH-PROBE absent Mark | new: `_coerce_numeric` returns a frame `.equals()` the inline result on `_april_df`/`_weekly_df` + tz-aware variant; AST: probe defines no own copy |
| 2 | `analytics_engine.py:35-59` | `_to_naive` already encapsulates tz-strip for scalars, but the **Series** tz-strip (`:58-59`, `:187-188` in probe) is hand-inlined `getattr(...dt,"tz",None)` twice. Naming/clarity drift only. | Add pure helper `_series_to_naive(s)` (exact same `if tz: tz_localize(None)` body) reused at `:58-59`. Provable no-op: identical condition + identical op. Probe mirror **deferred** (Sprint-23 byte-lock, same as #1). | **HIGH** (Sprint-22-locked tz path) | low | `test_sprint22_tz_regression.py` full-dict ×3 + `test_to_naive_identity_on_naive`/`test_to_naive_strips_tz_wall_clock_preserved` | new: `_series_to_naive` idempotent on naive Series; aware Series wall-clock preserved |
| 3 | `analytics_engine.py:138-143` & `:121-123` | `excluded` recomputed via `~bucket.apply(ec.is_stat_countable)` then re-split by `==STAT_BUCKET_ALGO`; `bucket.apply` is run for `countable`, `excluded`, and (implicitly) `manual` — 3 full passes over the same Series. Pure efficiency, output already pinned. | Compute `is_count = bucket.apply(ec.is_stat_countable)` and `is_algo = bucket==ec.STAT_BUCKET_ALGO` ONCE; derive `countable`/`excluded`/`manual`/`excl_algo`/`excl_manual` by boolean indexing on the cached masks. Same boolean values ⇒ identical frames. | **HIGH** (campaign partition feeds WR/Exp/PF/Net-R — CLAUDE.md core) | low-med | LOCKED `test_real_data_april_regression.py` (8/+$180.49, excl split 1m/1a; weekly 0/3a) + `test_sprint22_tz_regression.py` full-dict + `TestStatBucketExclusion`/`test_excluded_pnl_reported` | new: assert cached-mask path returns dict `==` current on April+weekly fixtures (full-dict) |
| 4 | `analytics_engine.py:380-387` `_get_closed_campaigns` docstring | Docstring says "campaigns whose **last SELL** falls in [start,end)". Actual code keys off **ANY** in-window SELL (`in_period["campaign_id"].unique()`), not the last/max SELL. Comment/behavior drift — misleading in the most-fragile aggregation path. | Docstring-only fix to "campaigns with **any** SELL in [start,end)". ZERO code change ⇒ trivially byte-identical. | **HIGH-area but ZERO-code** (doc only) | med (removes a correctness-reasoning trap for future edits) | All campaign tests unchanged by definition (no code touched) | none (doc) |
| 5 | `analytics_engine.py:419` | `entry_date = buys["trade_date"].iloc[0]` relies on `buys` already `sort_values("trade_date")` at `:394`; correct but the dependency is implicit/unnamed (`fb = buys.iloc[0]` reused at `:400`). Clarity only. | Rename/comment: `first_buy = buys.iloc[0]` (already `fb`); add one-line comment that `buys` is sorted so `iloc[0]`=earliest. No logic change. | **HIGH-area, comment-only** | low | LOCKED regression + `TestAggregateGetCampaignRiskMetrics` | none |
| 6 | `analytics_engine.py:415-417` | `orig_risk = true_orig_risk if true_orig_risk>0 else target_risk_usd` then `net_r=net_pnl/orig_risk`; `stat_bucket` uses `true_orig_risk`. Logic is correct & test-pinned but the fallback intent reads as a possible bug to a new maintainer. | Comment-only clarification (the existing 3-line comment is good but does not state the invariant "fallback never reaches a countable row because DATA_INCOMPLETE is excluded downstream"). Add that sentence. No code. | **HIGH-area, comment-only** | low | `test_missing_stop_campaign_is_data_incomplete_and_excluded` already proves the fallback row is excluded | none |

## Explicitly DO NOT TOUCH (not a provable no-op / not worth the risk)
- **`period_data_probe.py` bytes (any change).** Sprint-23 `test_period_data_probe_git_diff_empty` makes ANY probe edit a hard fail. Items #1/#2 probe-side are **DEFERRED** unless Mark explicitly reclassifies the Sprint-23 byte-lock. The duplication is real but the probe half is OUT.
- **`engine_core.py:478` coerce loop / `get_open_positions_campaign`.** Different column set, no `trade_date`, feeds the live open-book; folding into #1 risks the open-book path (`engine_core.py:479 notnull()` + WS-B). Not a provable identity with the analytics loop.
- **Any change to `_aggregate_campaigns` math, `get_campaign_risk_metrics`, `compute_original_campaign_risk`, `classify_stat_bucket`, `is_stat_countable`, `is_algo_position`, the WS-B unlinked block, `_empty`** — CLAUDE.md most-protected; no R/NAV/campaign/Expectancy change permitted (DEC-021). The `round(...,2)` in `compute_original_campaign_risk` and the `math.inf` PF branch are intentional; do not "clean up".
- **No dead/unreachable branch found** in the audited math path — the `closed_trades.empty`/`campaigns.empty`/`countable.empty` early returns are all reachable and test-pinned (`test_no_sells_in_period_returns_empty`, `test_all_excluded_returns_empty_with_disclosure`, weekly fixture). The ALGO "תקן entry/stop" wording on excluded rows is logged Sprint-21 behavior (DEC-020) — a behavior-change, OUT of a cleanup sprint.

## Recommendation (conservative)
Highest-confidence, smallest, fully-pinned: **#4 then #3** (doc-fix is zero-risk; #3 is pure-mask dedupe fully covered by the locked regression + full-dict tz equality). #1/#2 analytics-side only IF Mark rules the shared-helper extraction admissible AND the probe half stays out (Sprint-23 lock). #5/#6 comment-only, bundle if any code item is approved. Everything is fragile-area HIGH ⇒ no edit without explicit founder + Mark go-ahead.

— Engine team, Sprint-24 Wave-1 (DOC-ONLY; no code changed; no commit/push).
