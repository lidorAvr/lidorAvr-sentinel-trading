# Phase ALGO-2 — SCOPE / איפיון (T-B1 + T-C1 + T-C2) — founder-approved "הכל"

**Status:** SCOPE. Founder approved the FULL scope (D1 fix + the 4-gate risk-raise + the statistical-horizon decision). Governed, Mark-gated, parent-verified, exact-CI post-commit on the clean tree. The single biggest money-affecting risk-methodology Phase to date — built as ONE coordinated pass (all three are tightly coupled in `adaptive_risk_engine.py` + the stat-base data path; NOT parallelized). Source: `docs/teams/ALGO_INVESTIGATION_3.md` (D1/D2 CONFIRMED) + `ALGO_TEAM_CHARTER.md` doctrine ⟨memo⟩. Live HEAD `6e2a4d6`, suite 2145/0 cov 72.02%. No live financial values in any committed doc.

## Binding governance
NO byte-locked file modified: `analytics_engine.py`, `engine_core.py`, `period_data_probe.py`, LOCKED `tests/test_real_data_april_regression.py`, all `tests/_byte_lock_baselines/*`, `docker-compose.yml`, `telegram_bot_secure_runner.py`, migrations → git-diff EMPTY. **DEC-20260516-020 is INVIOLABLE: the report-period fetch `_fetch_trades_df` `weeks=8` and every report KPI / LOCKED April number stay byte-identical** — T-C2 adds a SEPARATE statistical-base read; it does NOT change the reporting window. ALGO stays observe-only (DEC-20260511-001 #8). Sprint-22/23/24 + C1/C2/B3/Arch-F1/NAV-Unify/W1/W3/ALGO-1/Sprint-30 invariants intact. Each workstream test-pinned + named proof; build → parent verify → exact-CI post-commit clean tree → push.

## T-B1 — D1 fix: never let ALGO contaminate the cold-start fallback (HIGH, safety, byte-identical on live path)
`adaptive_risk_engine.py:459-460` `if not disc_camps: disc_camps = closed_campaigns[:50]` is NOT `_is_disc`-filtered. Fix: the empty-fallback must remain disc/stat-countable-filtered (e.g. drop the unfiltered fallback entirely, or fall back only to disc-filtered campaigns). When there are genuinely **zero** stat-countable manual campaigns, the engine must enter an explicit **INSUFFICIENT-DATA state** (see T-C1) — it must NEVER feed ALGO into S9/M21/L50/heat/risk-raise. **Provably byte-identical on every path where `disc_camps` is non-empty** (the live normal path) — only the all-ALGO/empty cold-start path changes (to safe). LOCKED April byte-identical. Named proof: empty-disc ⇒ no ALGO in the heat base + INSUFFICIENT-DATA; non-empty ⇒ byte-identical.

## T-C1 — D2 fix: gate the risk-RAISE mechanism (the founder/Mark 4-gate, R-ALGO-1/6)
The display caveat warns the human but the mechanism (`heat_score ≥ 60 → direction="up" → +1 RISK_LADDER`) has NO sample/data gate. Build the 4-gate **on the risk-RAISE path ONLY** (drawdown auto-cut / "down" / "hold" UNCHANGED — protection must never be weakened): a step-up may fire ONLY if ALL hold —
1. **Clean data** — no Critical broker-reconciliation gap (reuse the existing recon classifier / band; "פער קריטי" ⇒ no raise).
2. **Sufficient sample** — stat-countable **manual** closed campaigns ≥ a Mark-doctrine floor (⟨memo⟩ "at least 20 relevant trades, not 9"); the INSUFFICIENT-DATA state from T-B1 ⇒ no raise.
3. **Positive expectancy** — window Expectancy ≥ the doctrine floor (⟨memo⟩ ~0.30R).
4. **Drawdown in control** — no recent loss-streak / drawdown-cut active.
If any gate fails: `direction` is clamped to at most "hold" (never "up"); emit an honest reason ("שער X נכשל — אין העלאת סיכון"). `_window_heat_score`'s `n==0 → 50.0` is replaced by an explicit INSUFFICIENT-DATA signal (not a pretend-neutral). RISK_LADDER values, the cut path, and `update_risk_pct` UNCHANGED. Money-affecting BUT strictly **risk-narrowing** (only ever blocks a raise, never enables one that was blocked, never weakens a cut) ⇒ provably no LOCKED-April/Sprint regression (those paths don't raise). Named proof: each gate-fail ⇒ no "up"; all-pass + heat≥60 ⇒ "up" preserved; cut/hold byte-identical; LOCKED April byte-identical.

## T-C2 — the statistical-horizon decision (chosen approach + rationale)
**Chosen (doctrine-aligned, stated explicitly for founder visibility):** decouple the **risk-raise statistical base** from the 8-week REPORT fetch. The report period stays `weeks=8` byte-identical (DEC-020). Add a SEPARATE, longer **rolling stat-base read**: the last up-to-50 **stat-countable MANUAL** closed campaigns drawn from a longer lookback (target: enough history that Gate-2's ≥20 and "L50" are actually reachable; implement as a dedicated longer-lookback query or an all-time manual scan, read-only, no Supabase mutation). S9/M21/L50 + Trust/Heat/the 4-gate compute off THIS base; the report's per-period KPIs (WR/Expectancy/PF/realized for the displayed period) stay exactly as today. Rationale: Mark's doctrine requires a *meaningful* sample to ever justify a raise; reusing the 8-week report window as the stat base makes that structurally impossible (Investigation #2/#3). Relabel-only was rejected as not "doing T-C2" (it's the Tier-A T-A1 clarity item, fold it in: the line now honestly states the true base + N). **Hard constraint:** zero change to any displayed report-period number / LOCKED April / Sprint-22-24 (the new read feeds ONLY the risk-raise/Trust/heat base). If a doctrine-correct longer base cannot be built read-only without touching a byte-locked path, STOP and report (re-scope T-C2; ship T-B1+T-C1 alone).

## Separate acceptance tests (`tests/test_phase_algo2.py`)
T-B1: empty/all-ALGO window ⇒ no ALGO in heat base + INSUFFICIENT-DATA; non-empty ⇒ byte-identical. T-C1: each of the 4 gates failing ⇒ no "up" (parametrised); all-pass+heat≥60 ⇒ "up"; drawdown-cut/"down"/"hold" byte-identical; the smallest-N-forces-up (N=3 all-win) now BLOCKED. T-C2: the stat base draws the longer manual rolling set; the report-period KPIs + LOCKED April byte-identical; "L50"/sample line honestly reflects the true base+N. No existing test deleted/weakened (Mark 6.1).

## Hard constraints (auto-FAIL)
Byte-locked files + baselines + migrations + compose + secure_runner git-diff EMPTY; report-period KPIs + LOCKED April + Sprint-22/23/24 byte-identical; ALGO observe-only; protection (cut/down/hold) never weakened; no Supabase mutation from the new read; no new alert/message TYPE. Full suite `python -m pytest -q` ≥ 2145, 0 failed (new tests only ADD). Exact CI command (CI env) post-commit on the clean tree → 0 failed, cov ≥67.

## Done = deploy-ready
T-B1+T-C1+T-C2 landed, parent-verified, full CI-equivalent post-commit clean-tree 0-failed, byte-locked + report-period numbers byte-identical, `docs/teams/PHASE_ALGO2_IMPL.md` written. Then return to the founder with ONE deploy command + the standing deferred reminder (L-1/tokens, OPS-1/2, F5).
