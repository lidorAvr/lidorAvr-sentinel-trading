# Phase REPORT-3 — SCOPE / איפיון (AI Master Context Export — FULL consolidation: units-lifecycle + adaptive-risk regime/trade-heat + ALGO-2A divergence + per-position engine action + portfolio aggregate — read-only, additive, single-source-of-truth, ALGO observe-only carve-out)

**Status:** SCOPE — founder explicitly chose FULL consolidation of the sidebar "🤖 AI Master Context Export" (`ai_str`, `dashboard.py:563-777`). The export is the text the founder copies into an external AI; it is today a strong but PARTIAL consolidation. This phase makes it complete by surfacing — READ-ONLY, ADDITIVE, via the EXISTING single-source formatters/engine entry points (ZERO new math) — the 5 missing blocks below. Governed, Mark-gated, parent-verified, exact-CI post-commit on the CLEAN tree. Live baseline branch `claude/review-system-audit-FBZ2h`, HEAD `67aa5fa` (post-REPORT-2 IMPL), suite **2314/0 cov 73.04%**. No live financial values in any committed doc. Author the SCOPE → founder approval → BUILD (tree left dirty) → independent parent verification → exact CI post-commit clean tree → push → IMPL.

## 0. Founder binding guidance (encoded literally, non-negotiable)
"The export is what I paste into an AI. It must centralize EVERYTHING — holdings, stats, risk regime, recommended actions — so the AI sees the whole picture." The deliverable answers EXACTLY this: the export must reach parity with the per-position card + the Telegram concentration report + the dashboard panels, **without re-implementing a single number**. Every added block REUSES the EXISTING formatter / engine entry point the visual surface and the Telegram report already use — the export becomes a faithful, byte-identical mirror, never a parallel re-computation. Accuracy over confidence (CLAUDE.md): where a block cannot be honestly verified (legs missing, sample too small, divergence side absent, recommendation unavailable) the export shows the EXISTING honest marker, never a fabricated value.

## 1. The gap — what is missing from `ai_str` (cross-team A — Export-structure/data-availability)
`dashboard.py:563-777` builds `ai_str` in 5 parts: the Observer Note, §1 Performance Matrix & Risk Profile, §2 Live Battlefield (the `actual_open_trades` loop, `dashboard.py:603-700`), §3 Execution Archive, §4 Next Required Decisions, then the NAV-freshness footer (`dashboard.py:773-775`) rendered at `st.sidebar.text_area(... value=ai_str ...)` (`dashboard.py:777`). It already carries: NAV/base capital, target risk, WR (disc + combined), ALGO count/PnL, DB net PnL, the canonical Risk-Capital-Basis line (`tf.fmt_risk_capital_basis`), the Broker-Reconciliation line (`tf.fmt_broker_reconciliation` — reusing the already-computed `_recon_status`), expectancy/RR, per-position dual-R/earnings/event-risk/state/sizing/protected-profit/giveback/capital-at-risk, and Next-Decisions heuristics. **CONFIRMED missing** (founder-chosen FULL consolidation):

1. **Units-lifecycle (Phase REPORT-2)** — per open position: original/remaining/realized units + %. **Insertion point:** inside the existing §2 loop `for _, row in actual_open_trades.iterrows():` (`dashboard.py:603-700`), as ONE additive line appended to the per-position block (immediately after the `State: … | Sizing: …` line at `dashboard.py:691`, before the conditional Protected-Profit/Capital-at-Risk lines). The single-source helper `position_lifecycle` is ALREADY imported as `plc` (`dashboard.py:19`); the per-campaign raw legs `_camp_legs_dash` are ALREADY built read-only at `dashboard.py:299-318` (REPORT-2). The loop variable `row['campaign_id']`/`row['quantity']` give the campaign key + the engine's authoritative `engine_net_qty`. Reuse `plc.compute_units_lifecycle(_camp_legs_dash.get(row.get('campaign_id')), engine_net_qty=qty)` + `plc.format_units_lifecycle(...)` — the SAME formatter the visual detail panel (`dashboard.py:869-876`) and the Telegram card (`telegram_portfolio.py:425-429`) already call (cross-surface byte-identity; honest-empty `— (לא ניתן לאמת)` preserved by construction — the helper decides).

2. **Adaptive-risk regime / trade-heat recommendation** — the Telegram "🎯 המלצת סיכון אדפטיבי / חום-מסחר NN/100 / S9·M21·L50 / העלאת-סיכון מדורגת" block. **Insertion point:** as a new export sub-section after §1 (or appended at the end of §1, before `## 🔭 2. Live Battlefield`), rendered from `tf.fmt_adaptive_risk_block(...)` (markdown `*…*` stripped to plain text the same way the existing Minervini-coaching reuse does, or left as-is — display sugar only). **HEADLINE FINDING — see §2.**

3. **ALGO-2A live↔backtest divergence observation** — the existing `algo_divergence` (ALREADY imported, `dashboard.py:18`) per-symbol line + footer for the live ALGO cohort, observe-only. **Insertion point:** a new export sub-section after §3 Execution Archive (or after §2), neutral 🔭, no directive — ALGO carve-out INVIOLABLE (AGENTS.md #8 / DEC-20260511-001). Reuse `algo_divergence.format_symbol_divergence_line` per symbol + `algo_divergence.format_divergence_footer()` ONCE — byte-identical to the dashboard ALGO panel (`dashboard.py:1273/1280`) and the Telegram ALGO panel. **DATA-AVAILABILITY caveat — see §6.**

4. **Per-position engine recommended action** — the engine's `action_short` (`build_management_action` via `evaluate_position_engine`, `engine_core.py:381/479`) is NOT in the export §2/§4. **Insertion point:** ONE additive token on the per-position block in §2 (alongside the existing `State:`/`Sizing:` line). **CONSISTENCY DECISION — see §4.**

5. **Portfolio aggregate summary** — total protected profit / giveback / total exposure % / ALGO-cluster exposure (the Telegram "📊 סיכום תיק הפיקוד" + "🤖 בקרת אשכול אלגו", `telegram_portfolio.py:550-559`). **Insertion point:** a new export sub-section after §2 Live Battlefield (or appended to §1). **CONFIRMED read-only available — see §5.**

## 2. HEADLINE FINDING — adaptive-risk read-only entry point (cross-team C — the headline risk)
**Existing recommendation producer:** `adaptive_risk_engine.compute_adaptive_risk(closed_campaigns, current_risk_pct, nav, ...)` (`adaptive_risk_engine.py:552`). **Existing renderer:** `telegram_formatters.fmt_adaptive_risk_block(risk_rec, settle_info=None)` (`telegram_formatters.py:291`) — verified PURE (string assembly only; reads dict keys; **no I/O, no Supabase, no state mutation, no engine call, never raises**). The Telegram report calls exactly this pair at `telegram_portfolio.py:633-642`.

**The risk:** `compute_adaptive_risk` is **NOT side-effect-free** — at `adaptive_risk_engine.py:798` it calls `_log_recommendation(result)` which **WRITES `risk_recommendations.json`** (a filesystem state mutation), and `drawdown_auto_cut_recommendation`/`get_risk_settle_info` read config files. A NEW `are.compute_adaptive_risk(...)` call from the export builder would therefore (a) re-mutate `risk_recommendations.json` and (b) re-enter risk/heat math — **both forbidden** (STOP conditions; AGENTS.md "Do not change R/NAV/exposure without tests"; CLAUDE.md fragile-area `adaptive_risk_engine.py`).

**DECISION — REUSE the ALREADY-COMPUTED result; ZERO new `are.` call (the headline mitigation).** The dashboard sidebar ALREADY calls `are.compute_adaptive_risk(_closed_for_rec, risk_pct_input, current_acc_size)` at `dashboard.py:491-492`, storing it in **`_risk_rec`**, for the existing "🎯 Adaptive Risk" widget (`dashboard.py:488-520`) — and that call happens **BEFORE** the `ai_str` builder begins at `dashboard.py:578`. The export block therefore **reuses the existing in-scope `_risk_rec` dict** (exactly the precedent the export already uses for `_recon_status` at `dashboard.py:596`, computed at `dashboard.py:472`) and calls ONLY the **pure** `tf.fmt_adaptive_risk_block(_risk_rec, settle_info=are.get_risk_settle_info())`. `are.get_risk_settle_info()` is a read-only config FILE READ (`adaptive_risk_engine.py:87-106`, no write) — the SAME read the Telegram report makes. **NET RESULT: zero new recommendation computation, zero additional `risk_recommendations.json` write, zero new risk-math, `adaptive_risk_engine.py` git-diff EMPTY.** This is the analogue of the REPORT-2 doctrine "callers compute / pass the formatted fragment in". If `_risk_rec.get("ok")` is False the export inherits the EXISTING honest path — `fmt_adaptive_risk_block` already renders `⚪ {msg}` ("not enough data" honest marker) — never a fabricated heat/recommendation (AGENTS.md #1).

**RED-LINE / STOP:** if BUILD finds the export builder cannot reach the already-computed `_risk_rec` without a NEW `are.compute_adaptive_risk(...)` call (e.g. scope/ordering change), the block is **DEFERRED** with an explicit honest scope note — it is NOT surfaced by re-calling the side-effecting engine. (Static analysis of `dashboard.py:491-578` confirms `_risk_rec` is in scope at line 578; the reuse path is available — but the fence stands.)

## 3. Honesty gates (cross-team A/D) — every block inherits an EXISTING honest marker
1. **Units (block 1):** the `position_lifecycle` helper's 4 honest-empty gates (no-rows / buys≤0 / sells>buys / net-recon-mismatch) are unchanged and authoritative — missing/ambiguous ⇒ `מחזור יחידות: — (לא ניתן לאמת)`, never a number (REPORT-2 §3, inherited verbatim). 2. **Adaptive-risk (block 2):** `_risk_rec["ok"]` False ⇒ `fmt_adaptive_risk_block` renders the existing `⚪ {msg}` honest marker; never a fabricated heat score (AGENTS.md #1). 3. **Divergence (block 3):** `algo_divergence`'s hard min-live-sample gate + the concrete-shortfall marker + the full honesty footer are emitted by the EXISTING formatter — below floor ⇒ "מדגם חי N/floor — חסרים M לסף אמין", never a delta. If no ALGO cohort/symbols ⇒ the EXISTING `INSUFFICIENT_LIVE_SAMPLE_HE` honest marker. 4. **Per-position action (block 4):** when the engine result is not OK the export prints the EXISTING engine error string verbatim + an honest note that the action is advisory and excludes the REPORT-2 partial-realize de-noising (see §4); never a fabricated directive. 5. **Aggregate (block 5):** the numbers are read verbatim from `live_df` / the existing exposure data — no recompute; the export inherits the EXISTING NAV-freshness footer (`dashboard.py:773-775`) + the already-present Broker-Reconciliation honesty line so no aggregate is asserted as exact beyond what the export already discloses. 6. **Never-replace:** every existing `ai_str` line/number is byte-identical pre/post — all 5 blocks are strictly ADDITIVE; no existing token is reordered, re-formatted, or recomputed.

## 4. Per-position engine action — the consistency decision (cross-team B/D — anti-drift)
**Context:** REPORT-2 added a SUPPRESSIVE-ONLY softening of `action_short` (non-ALGO, `ok=True ∧ realized>0`, never on critical/broken/never-suppress) that lives **INLINE** in `telegram_portfolio.py:476-505` (`_action_short_eff`). The export must not drift from the Telegram card.

**DECISION — Option (A) MINIMAL (chosen; smallest blast radius).** The export §2 surfaces the engine's **raw `action_short`** read-only, with an explicit honest note that it is the engine's advisory action and that the Telegram card additionally applies a display-only partial-realize de-noising (REPORT-2 §4) which the export does NOT replicate. `telegram_portfolio.py` and `telegram_formatters.py` stay **git-diff BYTE-EMPTY**. The export's §2 loop iterates `actual_open_trades` and does NOT currently call the engine; the canonical, lowest-risk source of the per-position action is the EXISTING `ec.evaluate_position_engine(...)` call ALREADY made inside `compute_live_portfolio_data` at `dashboard.py:196-199` — that result's `eval_res['data']['action']` is simply NOT yet stored on the `live_positions` dict (`dashboard.py:226-236`). **Sub-decision A1 (chosen):** add ONE additive read-only field `'Action': eval_res['data']['action'] if eval_res['ok'] else None` to the existing `live_positions.append({...})` dict (`dashboard.py:226-236`) — **NO new engine call, NO new math** (the `evaluate_position_engine` call already exists and is unchanged) — then surface `live_df`'s new `Action` column in §2 by joining on `CampaignId`/`Symbol` (the §2 loop already builds `_live_price_lookup` from `live_df` at `dashboard.py:602`; an analogous `Symbol→Action` lookup is read-only and additive). The honest note (block 4 §3 gate) discloses the export action is the engine raw advisory (no REPORT-2 softening) so a reader never assumes byte-identity with the softened Telegram "מה עכשיו" voice.

**WHY NOT (B):** extracting the REPORT-2 inline softening into a shared pure helper is cleaner anti-drift but RE-TOUCHES `telegram_portfolio.py` (a high-UX/safety file, AGENTS.md) and would have to PROVE every existing `telegram_portfolio` caller/test byte-identical for the softened path — non-zero risk for marginal benefit, since the export honestly DISCLOSES it shows the raw action (the de-noising is a display nicety on the phone, not a different directive; the raw action is the engine's truth). Option (B) is REJECTED unless BUILD discovers (A) is impossible without it (then STOP and escalate — do not silently switch to B).

## 5. Portfolio aggregate — read-only availability (cross-team A)
The Telegram "📊 סיכום תיק הפיקוד" totals (`telegram_portfolio.py:550-559`) are: total floating PnL, capital-loss risk (disc), realized-in-open campaigns, locked profit, secured profit, giveback risk, total exposure %, ALGO-cluster exposure %. **CONFIRMED the dashboard ALREADY holds these read-only:** `live_df['PnL'].sum()` (already computed at `dashboard.py:454` as `total_open_pnl`), `live_df['GivebackRisk']`/`LockedProfit`/`CapitalRisk`/`Exposure_USD`/`Exposure_Pct` (built in `compute_live_portfolio_data`, summed for the existing Heat-Map at `dashboard.py:813-833`), `current_acc_size` (NAV). The export builds these via simple `live_df[col].sum()` read-only aggregations (the SAME expressions the existing Heat-Map already uses at `dashboard.py:813-833` — zero new math, NEVER recompute R/NAV/exposure). **Single-source rendering:** prefer the EXISTING numbers and a plain ASCII/Hebrew aggregate block in `ai_str`; the Telegram aggregate is built inline in `telegram_portfolio.py` (no extractable pure formatter exists today), so the export composes the aggregate line from the SAME `live_df` columns (no string drift risk — these are factual sums, not a directive; the byte-identity mandate applies to the formatter-backed blocks 1/2/3, and to block 5 it is a "same-source-numbers" mandate, NOT a new shared formatter — extracting a Telegram aggregate formatter would re-touch `telegram_portfolio.py` and is OUT OF SCOPE / DEFERRED). NAV-freshness + Broker-Reconciliation honesty already in the export is inherited.

## 6. Single-source-of-truth / anti-drift mandate (cross-team B) — which EXISTING entry point each block reuses
| Block | EXISTING entry point reused (ZERO re-implementation in the export) | Cross-surface anti-drift |
|---|---|---|
| 1 Units-lifecycle | `plc.compute_units_lifecycle` + `plc.format_units_lifecycle` (`position_lifecycle.py`) — already imported `plc` | byte-identical to dashboard detail panel (`dashboard.py:869-876`) + Telegram card (`telegram_portfolio.py:425-429`) |
| 2 Adaptive-risk | the ALREADY-computed `_risk_rec` (`dashboard.py:491-492`) + pure `tf.fmt_adaptive_risk_block` + `are.get_risk_settle_info()` (read-only) | byte-identical to the Telegram report (`telegram_portfolio.py:633-642`); ZERO new `compute_adaptive_risk` call |
| 3 ALGO divergence | `algo_divergence.format_symbol_divergence_line` + `algo_divergence.format_divergence_footer()` — already imported | byte-identical to dashboard ALGO panel (`dashboard.py:1273/1280`) + Telegram ALGO panel |
| 4 Per-position action | the ALREADY-existing `ec.evaluate_position_engine` result inside `compute_live_portfolio_data` (`dashboard.py:196-199`), exposed as an additive `live_df['Action']` field — NO new engine call | raw engine `action_short`; honest note discloses it is un-softened vs the Telegram REPORT-2 de-noising |
| 5 Portfolio aggregate | EXISTING `live_df` columns + `current_acc_size`, summed exactly as the existing Heat-Map (`dashboard.py:813-833`) | same-source numbers (not a new formatter); Telegram aggregate stays inline/untouched |

**ALGO observe-only carve-out (INVIOLABLE — AGENTS.md #8 / DEC-20260511-001):** every block treats ALGO exactly as the source surfaces do. Block 1: an ALGO position MAY show the factual units line (no directive). Block 3: the divergence is observe-only (neutral 🔭, max "תצפית, לא איתות", never "דרושה פעולה"), NEVER fed into WR/Expectancy/PF, NEVER a recommendation. Block 4: for an ALGO position the engine already returns the externally-managed action ("מנוהל חיצונית — בקרה בלבד" / oversight-only); the export prints it verbatim and adds NO directive — Sentinel never issues/softens an ALGO exit/stop. Block 5: the ALGO-cluster exposure % is a factual exposure read-out (the EXISTING export §1 already prints ALGO count/PnL the same way). The Observer Note at the TOP of the export (`dashboard.py:579-582`) is unchanged and continues to fence ALGO.

## 7. Hard constraints — git-diff EMPTY (auto-FAIL)
`engine_core.py`, `analytics_engine.py`, `period_data_probe.py`, `risk_monitor.py`, **`adaptive_risk_engine.py` (READ-ONLY call of the ALREADY-invoked `_risk_rec` reuse + `get_risk_settle_info`; git-diff MUST be EMPTY — NO new `compute_adaptive_risk` call site, NO edit)**, `telegram_bot.py` (no wholesale rewrite; not touched), `telegram_tasks.py`, `supabase_repository.py` (used read-only only — unchanged), `position_lifecycle.py` (USE-not-modify), `algo_divergence.py`/`algo_metrics.py`/`algo_rules.py`/`algo_backtest_store.py` (USE-not-modify), LOCKED `tests/test_real_data_april_regression.py`, every `tests/_byte_lock_baselines/*`, `docker-compose.yml`, `telegram_bot_secure_runner.py`, migrations, `templates/*`, `docs/` (except the SCOPE/IMPL docs of THIS phase) → git-diff EMPTY.

**PERMITTED-DIFF allowlist (the ONLY files the build phase may touch):**
1. **`dashboard.py`** — additive ONLY: (a) the 5 export blocks inserted into `ai_str` at the §1/§2 points named in §1, each REUSING an existing formatter/entry point; (b) ONE additive read-only field `'Action'` on the EXISTING `live_positions.append({...})` dict (`dashboard.py:226-236`) sourced from the EXISTING `eval_res` (NO new engine call, NO new math); (c) any read-only lookup (e.g. `Symbol→Action`) built the same way the existing `_live_price_lookup` is. Every existing `ai_str` line/number/string + every existing `live_df` column byte-identical.
2. **NEW `tests/test_phase_report3.py`** (synthetic fixtures only; ADD-only).

`telegram_portfolio.py` and `telegram_formatters.py` stay **git-diff BYTE-EMPTY** (Option (A), §4). No schema/migration, no Supabase mutation, no new message TYPE, no new push/alert, ALGO observe-only intact, `adaptive_risk_engine.py`/`risk_monitor.py` NOT touched.

**Option (B) RULING — REJECTED as a touch-point.** Team D: extracting the REPORT-2 inline softening into a shared helper re-touches `telegram_portfolio.py` (high-UX/safety) with no honesty benefit — the export honestly discloses it shows the raw engine action. The export is scoped to the **raw engine `action_short` + an honest divergence-disclosure note**, `telegram_portfolio.py` git-diff EMPTY — STOP condition if BUILD finds (A) impossible without (B).

**`adaptive_risk_engine.py` RULING — REUSE-not-recompute (the headline mitigation, §2).** Touching `adaptive_risk_engine.py` or adding a new `compute_adaptive_risk` call would (a) re-mutate `risk_recommendations.json` and (b) re-enter risk/heat math — both forbidden. The block reuses the ALREADY-computed `_risk_rec` (`dashboard.py:491-492`, in scope at `dashboard.py:578`) + the pure `tf.fmt_adaptive_risk_block` — STOP condition if BUILD finds it must re-call the engine.

## 8. Proof obligations (named — verified independently by parent)
(a) **read-only intact** — `dashboard.py` adds NO new `are.compute_adaptive_risk` call (grep/AST: the only `compute_adaptive_risk` call site stays `dashboard.py:491-492`); NO new `ec.evaluate_position_engine` call (the only added engine touch is reading `eval_res['data']['action']` from the EXISTING call); zero push/directive/Supabase write/state mutation/new message TYPE; `risk_recommendations.json` write-count unchanged (still exactly the one existing dashboard call). (b) **single-source byte-identity** — each block's export text == the originating surface's text from the SAME formatter on the SAME input: units (export==dashboard detail==Telegram card), divergence (export==dashboard ALGO panel==Telegram ALGO panel), adaptive-risk (export `fmt_adaptive_risk_block(_risk_rec)` == Telegram `fmt_adaptive_risk_block(risk_rec)` for an identical `risk_rec`). (c) **honest-empty proven** — units missing/ambiguous ⇒ `— (לא ניתן לאמת)`; `_risk_rec.ok=False` ⇒ `⚪ {msg}`; divergence below floor / no cohort ⇒ the existing concrete-shortfall / `INSUFFICIENT_LIVE_SAMPLE_HE` marker; per-position engine not-OK ⇒ engine error verbatim + honest note — NEVER a fabricated number/directive (each gate a test). (d) **ALGO observe-only intact** — divergence neutral 🔭, no imperative/no 🔴🟢, never feeds WR/Expectancy/PF; ALGO position action printed verbatim with NO added directive; the export Observer Note + ALGO branches byte-identical. (e) **no risk-math change** — a managed campaign's R/NAV/exposure/heat/adaptive-risk figures byte-identical pre/post; the aggregate block equals the EXISTING `live_df`/Heat-Map sums (no recompute). (f) **no Supabase / state / new TYPE** — `git diff` shows no schema/migration/Supabase write; no new message TYPE/callback/push. (g) existing report KPIs + **LOCKED April byte-identical** (8 / +$180.49 / WR .375 / PF 2.6262 / excl 2); every existing `ai_str` line/number + every existing `live_df` column + existing dashboard/`telegram_portfolio`/`telegram_formatters`/ALGO-2A/REPORT-2 tests byte-identical/green unchanged (only additive export lines + ONE additive `live_df` field). (h) full suite `python -m pytest -q` ≥ **2314 + Δ, 0 failed** (new tests ADD-only; no existing test deleted/weakened — Mark 6.1). (i) **exact CI post-commit on the CLEAN tree:** `TELEGRAM_BOT_TOKEN=ci-test-token TELEGRAM_ADMIN_ID=12345 SUPABASE_URL=https://ci-test.supabase.co SUPABASE_KEY=ci-test-key DEV_PIN=0000 python -m pytest -q` → 0 failed; CI workflow's `pytest --tb=short -q --cov=engine_core --cov=adaptive_risk_engine --cov=analytics_engine --cov=addon_risk_engine --cov-report=term --cov-fail-under=67` → cov ≥ 67 (currently 73.04%).

## 9. Separate acceptance tests (`tests/test_phase_report3.py`, synthetic fixtures only)
All export-builder logic is tested via small extractable pure helpers / synthetic `live_df`/`actual_open_trades`/`_risk_rec`/`_camp_legs` fixtures (no real symbols/quantities/P&L; no network/Supabase). Coverage: block 1 — export units line == `plc.format_units_lifecycle` output for the SAME input (byte-identity vs the dashboard-detail/Telegram path) incl. each honest-empty gate ⇒ `— (לא ניתן לאמת)`. Block 2 — export adaptive-risk text == `tf.fmt_adaptive_risk_block(_risk_rec, settle_info)` for an identical `risk_rec` (byte-identity vs Telegram); `_risk_rec.ok=False` ⇒ `⚪ {msg}` honest marker; **a test asserts the export builder makes NO `are.compute_adaptive_risk` call (the side-effecting producer is the existing one)** — `risk_recommendations.json` write-count invariant. Block 3 — export divergence text == `format_symbol_divergence_line`+`format_divergence_footer` (byte-identity vs the dashboard ALGO panel); below-floor ⇒ concrete shortfall, never a delta; no imperative/no 🔴🟢; ALGO never feeds WR/Expectancy. Block 4 — export action == the raw engine `action_short` from the EXISTING `eval_res`; ALGO ⇒ verbatim externally-managed action + NO added directive; engine not-OK ⇒ error verbatim + the honest un-softened-vs-Telegram note. Block 5 — aggregate == the EXISTING `live_df`/Heat-Map sums (no recompute). Cross-cutting: every existing `ai_str` line/number byte-identical pre/post (never-replace); the ONLY added `live_df` field is `Action`, every existing column byte-identical; the export builder imports no NEW side-effecting symbol; LOCKED April + all `tests/_byte_lock_baselines/*` unchanged; existing dashboard/`telegram_portfolio`/`telegram_formatters`/REPORT-2/ALGO-2A tests pass unchanged. No existing test deleted/weakened (Mark 6.1); any bug-codifying test, if present, is CORRECTED not weakened.

## 10. Deferral list
**Phase REPORT-3 does NOT:** send any Telegram/digest/push alert; add a new message TYPE; touch `risk_monitor.py`/anti-spam state; touch `adaptive_risk_engine.py` (no new `compute_adaptive_risk` call — reuse the existing `_risk_rec`); mutate Supabase; add a directive/auto-management/threshold-action; change R/NAV/exposure/campaign/heat/adaptive-risk math; touch ALGO observe-only; re-implement ANY number in the export (every block reuses the existing formatter/entry point); extract the REPORT-2 inline softening into a shared helper (Option (B) — DEFERRED, only if a future phase proves it zero-risk for every `telegram_portfolio` caller); extract a shared Telegram-aggregate formatter (block 5 uses same-source numbers, not a new formatter — DEFERRED). **Standing deferred reminders:** 🔴 SECURITY L-1 + Telegram token rotation; OPS-1; OPS-2; the open Phase ALGO-2A IMPL follow-up (unify the upstream live-aggregate builder into one shared read-only helper); a future "extract per-position-action softening into a shared pure helper" idea (Option B) → a separate governed, founder-gated phase.

## 11. STOP conditions (parent-verification contract)
The build phase MUST STOP and escalate to the CEO (not force the scope) if ANY becomes true:
- the adaptive-risk block cannot be surfaced by REUSING the already-computed `_risk_rec` and would require a NEW `are.compute_adaptive_risk(...)` call (re-mutates `risk_recommendations.json` / re-enters risk-heat math) — STOP; DEFER block 2 with an explicit honest scope note rather than re-call the side-effecting engine;
- surfacing any block would require recomputing R / NAV / exposure / campaign / heat / adaptive-risk math, a Supabase write, a state mutation, a new message TYPE, or a new push/alert;
- any byte-locked file (§7) — especially `adaptive_risk_engine.py`, `engine_core.py`, `risk_monitor.py`, `position_lifecycle.py`, `algo_divergence.py`, `telegram_portfolio.py`, `telegram_formatters.py` — would need a non-empty diff;
- Option (A) for the per-position action proves impossible without Option (B) (re-touching `telegram_portfolio.py`) — STOP, do not silently switch to (B);
- the ALGO observe-only carve-out cannot be held (any block would add/soften/edit an ALGO directive, or feed ALGO into WR/Expectancy/PF);
- an existing test would need to be weakened/deleted to pass (Mark 6.1 — correct, never weaken; if a genuine bug-codifying test exists, CORRECT it and flag it, do not weaken);
- cross-surface byte-identity (export == originating surface from the single formatter) cannot be proven for blocks 1/2/3.

## 12. Done = deploy-ready
All 5 blocks landed, parent-verified, full CI-equivalent post-commit on the CLEAN tree 0-failed, cross-surface byte-identity (blocks 1/2/3) + same-source-numbers (block 5) + read-only intact (zero new `compute_adaptive_risk`/engine call, `risk_recommendations.json` write-count unchanged) + honest-empty inherited + ALGO observe-only carve-out + no risk-math change + LOCKED April + report KPIs proven, no byte-locked / `adaptive_risk_engine.py` / `risk_monitor.py` / `telegram_portfolio.py` / `telegram_formatters.py` / `supabase_repository.py` / `telegram_bot.py` touched, no real trade data committed, `docs/teams/PHASE_REPORT3_IMPL.md` written. Then return to the founder with ONE deploy command (rebuild the **`dashboard`** service only — the change is `dashboard.py`-only; `telegram-bot` is NOT touched) + the standing deferred reminders + the open Option-(B)/aggregate-formatter follow-up note.
