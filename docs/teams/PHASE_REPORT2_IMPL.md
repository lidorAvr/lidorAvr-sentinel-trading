# Phase REPORT-2 — IMPL / יישום (position units-lifecycle: original/remaining/realized + % — dual-surface single-formatter; suppressive-only decision-awareness; ALGO observe-only carve-out)

**Status:** LANDED — parent-verified, exact-CI post-commit on the CLEAN tree 0-failed. Scope: `docs/teams/PHASE_REPORT2_SCOPE.md` (governs). Code HEAD `feafbdc` on `claude/review-system-audit-FBZ2h` (scope `164b20c`, baseline `da40aed` post-ALGO-2A.1). No live financial values / no real trade data in any committed file.

## What landed
- **NEW `position_lifecycle.py`** — pure deterministic leaf, imports ONLY `typing` (verified zero `engine_core`/`analytics_engine`/`period_data_probe`/`risk_monitor`/Supabase/network — AST + subprocess introspection tests). `compute_units_lifecycle(campaign_rows, *, engine_net_qty)` re-derives Σ|BUY| (`original`) / Σ|SELL| (`realized`) / `net` (`remaining`) / `realized_pct` READ-ONLY from the SAME raw legs the byte-locked engine splits — mirrors `engine_core.split_side_first` doctrine exactly (`str(side).upper().strip()` ∈ {BUY,SELL}; `abs(quantity)`; exact-`trade_id` dedup guard per DATA_CONTRACTS F4). `format_units_lifecycle(lc)` is THE single source of truth — both surfaces emit byte-identical text. Never raises.
- **Four honest-empty gates (AGENTS.md #1 — absence ≠ data):** no-rows ⇒ `_empty("no_rows")`; `buys_qty<=0` ⇒ `buys_qty_le_0`; `sells>buys` ⇒ `sells_gt_buys`; re-derived net vs the engine's own `quantity` mismatch > `RECON_TOL=0.001` (tolerance pinned == the engine's by a test) ⇒ `net_recon_mismatch`. Every empty path ⇒ `ok=False`, numbers `None`, rendered `‏מחזור יחידות: — (לא ניתן לאמת)` — NEVER a fabricated number / silent zero. The reconciliation gate is the fail-safe backstop: any leg-split imperfection collapses to honest-empty, never a wrong number. Valid ⇒ `‏מחזור יחידות: מקורי 15 · נותרו 9 · מומשו 6 (40%)`.
- **Dual-surface ADDITIVE only:** `telegram_portfolio.py` inserts ONE lifecycle line into the non-ALGO discretionary card (existing `quantity`/`כמות` byte-identical, never replaced). `dashboard.py` adds ONE marker-delimited `st.caption("📦 [units-lifecycle] …")` element in the per-position detail (every existing `Qty`/column/number unchanged). Both call the SAME `plc.format_units_lifecycle` (cross-surface byte-identity test). `telegram_formatters.py` left **byte-EMPTY** (caller appends — no signature change).
- **Suppressive-only decision-awareness — the 4 inviolable fences (SCOPE §4):** the softening lives entirely in the non-ALGO `else` branch of `handle_portfolio_room` and operates ONLY on the already-computed display string `_action_short_eff` passed into `fmt_position_card`. (a) **ALGO carve-out:** the ALGO branch is git-diff-untouched — no suppression, no `_action_short_eff`, no lifecycle line (observe-only, DEC-20260511-001). (b) **ZERO risk-math change:** never re-enters the engine; `total_campaign_r`/`open_r`/`original_campaign_risk`/NAV/exposure/heat byte-identical. (c) **No new directive/TYPE/callback/push:** only replaces a redundant realize/trim/Runner voice with the neutral honest note `כבר מומש חלקית — אין צורך לממש שוב כרגע`. (d) **Ambiguity ⇒ existing behaviour:** fires ONLY when `ok=True ∧ realized>0`; on `ok=False` the existing `action_short` renders verbatim; a `🚨 קריטי`/`🔴 Broken`/`🚨 חריגת סיכון אלגו` status or an emergency-exit action (`יציאה מיידית 🚨` etc.) is NEVER suppressed.

## Proof obligations — verified (parent, independent)
- `git diff --name-only` ⇒ exactly `dashboard.py`, `telegram_portfolio.py` (+ NEW `position_lifecycle.py`, `tests/test_phase_report2.py`). Protected set git-diff EMPTY: `risk_monitor.py`, `engine_core.py`, `analytics_engine.py`, `period_data_probe.py`, `adaptive_risk_engine.py`, `supabase_repository.py`, `telegram_bot.py`, `telegram_tasks.py`, `telegram_formatters.py`, `algo_*`, LOCKED April, all `tests/_byte_lock_baselines/*`, `docker-compose.yml`, `telegram_bot_secure_runner.py`, migrations, `templates/*`, `docs/`.
- Independent full suite (CI env): **2314 passed / 0 failed** (2271 baseline + 43 ADD-only; no existing test weakened — Mark 6.1). Test classes 1–11 cover: derivation correctness, all honest-empty gates incl. recon-tolerance pin, trade_id dedup, valid line format, cross-surface byte-identity, suppressive-only (does-not-fire-on-ok=False; BROKEN/stop-breach/critical never suppressed), ALGO observe-only intact, never-replace, import-purity, determinism, LOCKED-April-&-byte-lock unaffected.
- Exact CI command POST-COMMIT on the **clean tree**: `2314 passed`, **coverage 73.04% ≥ 67%**, 0 failed.
- No schema/migration, no Supabase mutation, no new message TYPE, ALGO observe-only intact, a managed campaign's risk/R/NAV/exposure byte-identical pre/post, LOCKED April byte-identical (8 / +$180.49 / WR .375 / PF 2.6262 / excl 2). No real trade data committed (synthetic leg dicts only). No STOP condition triggered; `risk_monitor.py` confirmed unnecessary (display-layer suppression sufficient).

## Deploy
Additive read-only display + a display-layer string softening on two existing pull-only surfaces; production wiring unchanged (`docker-compose.yml` byte-identical). Rebuild **both** `dashboard` + `telegram-bot` (the change touches `dashboard.py` and `telegram_portfolio.py`). Effect: every non-ALGO position card / dashboard detail now shows `מחזור יחידות: מקורי X · נותרו Y · מומשו Z (P%)`; when legs are missing/ambiguous/non-reconciling it honestly shows `— (לא ניתן לאמת)` instead of guessing; on a non-ALGO campaign already partially realized a redundant "ממש/הדק" suggestion is replaced by the neutral note (risk/critical/broken actions and all ALGO output unchanged).
